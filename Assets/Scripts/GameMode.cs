/// Ersan - 150918 - An enum for the game mode: forwards(inputting the digits in normal order), reversed(inputting the digits in reverse order).

public enum GameMode
{
    Forwards = 0,
    Reversed = 1,
}
